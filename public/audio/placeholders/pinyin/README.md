# 拼音音频占位素材

本目录只存放开发期占位音频，不属于可发布课程素材。

## ma-four-tones.mp3

| 字段 | 值 |
|---|---|
| 用途 | 四声课程跟读交互的前端链路验证 |
| 生成日期 | 2026-08-20 |
| 生成方式 | macOS `Tingting` 系统 TTS，输入“妈。麻。马。骂。” |
| 内容来源 | 项目内生成；未读取或复制 `docs/textbooks` |
| 版权状态 | 开发占位，不作为原创真人发音素材 |
| 发布状态 | `mustReplaceBeforePublish: true` |
| 替换要求 | 发布前替换为本项目原创录制或明确授权的真人普通话示范 |

课程数据中的 `contentOrigin`、`placeholder` 和 `mustReplaceBeforePublish`
必须与本清单一致。Schema 会拒绝缺少替换标记的生成占位音频。
